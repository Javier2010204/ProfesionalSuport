json.extract! contract, :id, :place, :name, :nacionality, :profession, :academy_degree, :university, :departament, :dpi, :age, :marital_status, :email, :phone, :created_at, :updated_at
json.url contract_url(contract, format: :json)
